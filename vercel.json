{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api",
      "methods": ["GET", "POST", "DELETE", "PUT"]
    },
    {
      "src": "/(?!api/)[^.]+$",
      "dest": "/index.html"
    }
  ]
}
